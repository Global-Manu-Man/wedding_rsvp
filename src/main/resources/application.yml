# =====================================================
# CONFIGURACION DE PRODUCCION - RAILWAY
# =====================================================
# Usa variables de entorno de Railway automaticamente
# =====================================================

spring:
  application:
    name: wedding-rsvp-service

  # =====================================================
  # CONFIGURACION DE BASE DE DATOS CON VARIABLES DE ENTORNO
  # =====================================================
  datasource:
    # Railway proporciona estas variables automaticamente
    url: jdbc:mysql://yamanote.proxy.rlwy.net:26043/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: IejWHmkMNfyXMLpWNldOACCWbOWViXtl

    # Pool de Conexiones HikariCP
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # =====================================================
  # CONFIGURACION JPA
  # =====================================================
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate:
        # Railway usa MySQL, no especificar dialecto (se detecta automaticamente)
        # dialect: org.hibernate.dialect.MySQLDialect  # Comentado - se auto-detecta
        format_sql: false
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true

  # Desactivar H2
  h2:
    console:
      enabled: false

# =====================================================
# SERVIDOR
# =====================================================
server:
  # Railway asigna el puerto automaticamente
  port: ${PORT:8080}
  servlet:
    context-path: /api
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html
    min-response-size: 1024

# =====================================================
# LOGGING
# =====================================================
logging:
  level:
    root: INFO
    com.wedding: INFO
    org.springframework.web: WARN
    org.hibernate: WARN

# =====================================================
# PAGINACION
# =====================================================
application:
  pagination:
    default-page-size: 10
    max-page-size: 100